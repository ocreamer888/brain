---
description: 
alwaysApply: true
---

## Learned User Preferences
- Communicate in a caveman style to save tokens; personal Cursor skill `caveman` lives at `~/.cursor/skills/caveman/` (lite/full/ultra via `/caveman` or trigger phrases).
- May write in Spanish or English; match the conversation language when practical.
- When asking to connect or configure tooling (e.g. MCP), expects the assistant to apply changes in the repo and environment—not only step-by-step instructions—when edits are allowed.
- Prefers a brief approach check-in before major code changes and explicit approval before large edits; for multi-phase written implementation plans (e.g. superpowers executing-plans), prefers pausing at each phase boundary for review before starting the next phase.
- Prefers the smallest practical change; avoid over-engineering and avoid assumptions by asking when uncertain.
- Use this Shared/Code checkout’s own `brain/rust/brain.db`; do not default to or link against the Documents/AI corpus. Additional focused instances should live outside the repo under `~/.brain/instances/` (code ≠ data), not as cloned checkouts.
- When bringing external markdown corpora (e.g. Obsidian vault material) into this project, prefers **copying** the needed trees **into the repo** (e.g. under `vault/`) instead of symlinked vault roots or a long-term workflow that depends on absolute `Documents/...` paths as the canonical ingest source; for PARA-style imports, keep **`02 Areas`** alongside projects/docs as a first-class bucket.
- For project memory over docs, favors **stronger retrieval + evals + direct access to full source files** kept in-repo, with chunked embeddings as an **indexing layer** rather than the only retained copy of the text.
- Uses this brain as a **personal long-horizon system** (not a disposable chat log): preserve context across work, knowledge, creativity, and life so patterns can surface over time.
- Before building automation that assumes corpus/DB/graph state is correct, **verify upstream data matches reality first** (avoid building then discovering misalignment).
- When behavior is described as production-ready, prefers explicit **live** verification of critical paths (not only mock-based tests), especially for LLM-powered summarization (Ollama on Mac Studio; OpenRouter still relevant for non-Ollama paths).
- For re-ingestion and migrations, prioritizes **zero data loss**; keep raw knowledge and enrich/repair metadata instead of dropping source chunks.

## Learned Workspace Facts

- The **brain** MCP server (`user-brain` / `brain` in Cursor) lives under `brain/mcp/`; requires Rust `brain_api` on 8787 — if down, MCP calls fail with connection refused. Cursor reads **`.cursor/mcp.json`** (not only repo `.mcp.json`); point launchers at this repo’s `brain/mcp/run_server.sh` (not Documents/AI). Tools: `search_brain`, `save_memory_tool`, `get_context_tool`, `reflect_tool`, `get_stats_tool`, `record_feedback`.
- Production vector index is **in-process, rebuilt from SQLite embedding BLOBs** at startup (`brain/rust/brain.db`); embeddings are **ONNX `all-mpnet-base-v2`** (not Ollama). Model dir `brain/rust/models/all-mpnet-base-v2-onnx/` is **gitignored** (~417MB) — export **locally** via `python3 brain/tools/export_onnx.py` (Hugging Face download); **no** GitHub Actions ONNX/embedder workflow (removed; local-only). Smoke without a model: `BRAIN_EMBEDDER=mock`. `brain/db` Chroma store is **legacy**; bootstrap SQLite may exist at `brain/bootstrap/cursor_brain.db`.
- **Operational brain** for this workspace is `/Users/Shared/Code/brain` (Rust + Python + MCP) with SQLite at `brain/rust/brain.db`. First `brain_api` start **auto-creates** the DB file + schema (`CREATE TABLE IF NOT EXISTS`); empty `0 memories` is normal. Documents/AI is a separate legacy corpus/binary plane—not this checkout’s default data plane. **Claw** material (`claw/brain`, `reference-claw-code-main/rust`, etc.) is **reference-only**. **Multi-instance (locked):** separate SQLite DBs (hard walls) under `~/.brain/instances/` + JSON registry; one active brain; hot-switch inside a single `brain_api` (rebuild in-memory index; MCP stays on 8787); Instances tab v1 = create/list/rename/switch/active + archive-delete + description/tags (no folders); register today’s `brain.db` as Main (path may stay until migrated).
- Full Brain Viewer: built assets under `brain/rust/static/` (SPA served by `brain_api`); Vite/React source under `brain/rust/ui/`.
- This project is primarily **local** (do not add CI that downloads/runs the ONNX embedder on GitHub); `BRAIN_BACKEND=api` is the production golden path; `BRAIN_BACKEND=python` is manual-QA-only (documented in `brain/mcp/server.py`).
- **`brain_api`** release binary is managed by a **launchd job `com.brain.api`** that auto-restarts after `pkill` (to permanently stop: `launchctl bootout gui/$(id -u)/com.brain.api && launchctl disable gui/$(id -u)/com.brain.api`; do not `pkill` alone). **Plist Program/WorkingDirectory/`BRAIN_DB_PATH` must target this Shared/Code checkout** — a Documents/AI-targeted job serves the wrong corpus (missing Linked graph / wrong instance). If `brain/rust/brain.db` is corrupt (`database disk image is malformed`, e.g. truncated to ~110 KB) the job **spawn-loops**, and a still-running old binary can **mask the corruption until a restart exposes it**. Recover: `launchctl bootout` to stop the loop, snapshot the bad db to `brain/rust/corrupt-snapshot-*/`, then restore the newest good **dated backup** `brain.db.bak-YYYYMMDD-HHMMSS`.
- The web viewer at `http://127.0.0.1:8787/` requires **`BRAIN_API_AUTH_REQUIRED=0`** for browser use — browser JS does not send `x-api-key`, so `/v1/stream` and `/v1/search_index` return `401` with auth enabled.
- Production `brain_api` `Brain::search` ranks with **weighted cosine + BM25 hybrid** (default `BRAIN_HYBRID_ALPHA` / `hybrid_alpha` ≈ 0.7, plus light recency weighting); **FTS5** via `memories_fts` stays synced on memory upsert/delete. **RRF** fusion lives only in **offline eval** (`brain/tools/retrieval_eval_kfold.py`), not in the live API ranker.
- **`brain/ingest/`** is the canonical Python ingest library — ALL Python ingest paths must import from here, never implement chunking/tagging inline. Key modules: `text_chunking.py` (`chunk_by_sections`, `chunk_by_paragraphs`, `refine_chunks_for_word_limit`; word budget via `OBSIDIAN_CHUNK_WORDS` env, default 1500), `payloads.py` (`TAG_BRAIN_INGEST`, `with_ingest_tag`, `format_bracketed_memory`, `memory_display_title`).
- **Brain Fact Layer Phases 1–4 shipped** (May 2026, commit `5f7e24e`): `MemoryType::{Fact, Episode}`, 5 new `MemoryMetadata` fields, `curation_events` + `backfill_batches` tables (Phase 1); `fact_extractor.py` + `fact_curator.py` with dedup/merge gate (Phase 2); ingest path wiring in `session_end.py` + `ingest_session_chunks.py` (Phase 3); `SearchFilter.exclude_superseded: bool` defaults `true` + MCP `get_episode` tool (Phase 4). **Phase 5** (backfill) is checkpoint-resumable via `python3 brain/tools/backfill_facts.py --all`; `salience` stored but NOT used in retrieval until Phase 6.
- **LLM on Mac Studio is Ollama-first:** `brain_api` uses `BRAIN_LLM_PROVIDER=ollama` + `OLLAMA_MODEL`; Python facts/summarize use `OLLAMA_URL` / `OLLAMA_SUMMARIZE_MODEL` in `brain/config.py` (`fact_extractor.py`, `fact_curator.py`). OpenRouter env lookups remain in Rust `summarizer.rs` for non-Ollama providers only.
- **Entity-edge graph SHIPPED** (`entities` + `edges` SQLite tables, relation `"mentions"`; 1-hop `graph_expand` default **OFF**): endpoints `/save` (entities), `GET /entities`, `GET /neighbors`, `POST /link-entities`, MCP `get_neighbors_tool`; Linked tab is a bipartite memory↔entity canvas. Product scope locked to **all memory types except `episode`**; today auto-extract + `backfill_entities.py` are still **fact-scoped** until extended. Long-run/tool gotchas: run Python tools under repo **`.venv`**; brain API returns **HTTP 429** on save bursts; launch long jobs as harness-managed background jobs (`block_until_ms=0`), not `nohup &`.
